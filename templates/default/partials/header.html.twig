
<div class="navbar">
    <ul style="display: flex;justify-content: end;list-style-type: none;">

        <li style="margin-right: 10px;"><a href="{{ path('index') }}">Acceuil</a></li>


        {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') and not app.user%}
            <li style="margin-right: 10px;">
                <a href="{{ path('app_register') }}">Inscription</a>
            </li>
        {% endif %}
        {% if app.user  %}
            <li style="margin-right: 10px;">
                <a href="{{ path('app_logout') }}">Déconnexion</a>
            </li>
        {% else %}
            <li>
                <a href="{{ path('app_login') }}">Connexion</a>
            </li>
        {% endif %}
    </ul>
</div>