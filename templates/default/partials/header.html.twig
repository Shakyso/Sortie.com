
<div class="navbar">


    <div class="navbar-logo">
        <p>LOGO ICI</p>
    </div>


    <div>
        <div class="navbar-link">
            <ul class="navbar-ul">

                <li class="menu-li"><a href="{{ path('index') }}">Acceuil</a></li>

                {#TODO affichage en fonction des roles#}

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="menu-li">
                        <a href="{{ path('site_list') }}">Les sites</a>
                    </li>

                    <li class="menu-li">
                        <a href="{{ path('ville_list') }}">Les villes</a>
                    </li>
                {% endif %}


                {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') and not app.user %}
                    <li class="menu-li">
                        <a href="{{ path('app_register') }}">Inscription</a>
                    </li>
                {% endif %}
                {% if app.user  %}
                    <li class="menu-li">
                        <a href="{{ path('security_account', {'id' : app.user.id }) }}">Mon profil</a>
                    </li>
                    <li class="menu-li">
                        <a href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>


                {% else %}
                    <li class="menu-li">
                        <a href="{{ path('app_login') }}">Connexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>


</div>