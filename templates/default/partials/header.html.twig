



<div class="navbar">



    <div class="nav-parent">

        <ul class="navbar-ul">

            <li class="menu-li"><img src="{{ asset('assets/img/logo.png') }}" alt="le logo" width="4%"></li>

            <li class="menu-li"><a href="{{ path('index') }}">Accueil</a></li>



            {% if is_granted('ROLE_ADMIN') %}
                <li class="menu-li">
                    <a href="{{ path('site_list') }}">Les sites</a>
                </li>

                <li class="menu-li">
                    <a href="{{ path('ville_list') }}">Les villes</a>
                </li>
            {% endif %}


            {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') and not app.user %}
                <li class="menu-li">
                    <a href="{{ path('app_register') }}">Inscription</a>
                </li>
            {% endif %}
            {% if app.user  %}
                <li class="menu-li">
                    <a href="{{ path('security_account', {'id' : app.user.id }) }}">Mon profil</a>
                </li>
                <li class="menu-li">
                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                </li>


            {% else %}
                <li class="menu-li">
                    <a href="{{ path('app_login') }}">Connexion</a>
                </li>
            {% endif %}
        </ul>

    </div>


</div>